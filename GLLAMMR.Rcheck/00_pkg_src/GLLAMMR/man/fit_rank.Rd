% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank.R
\name{fit_rank}
\alias{fit_rank}
\title{Fit Rank-Ordered Logit Models with Taste Heterogeneity}
\usage{
fit_rank(
  formula,
  case,
  data,
  random = NULL,
  weights = NULL,
  start = NULL,
  control = list()
)
}
\arguments{
\item{formula}{Model formula \code{rank ~ x1 + x2} where the response is
the rank of each alternative within its case (1 = most preferred).
Unranked alternatives may be coded \code{NA}; they remain in every
choice set without contributing a stage.}

\item{case}{One-sided formula naming the case identifier, e.g. \code{~ id}}

\item{data}{Data frame in long format (one row per alternative per case)}

\item{random}{Optional one-sided formula \code{~ (0 + attribute | group)}
giving the attribute carrying the random coefficient and the grouping
variable. NULL fits a fixed-effects rank-ordered logit (the random SD
is fixed near zero).}

\item{weights}{Optional one weight per case (matched via the case id)}

\item{start}{Optional starting values}

\item{control}{Optimization control list}
}
\value{
An object of class \code{gllamm_rank}
}
\description{
Fits an exploded (rank-ordered) logit model for ranking data, with an
optional group-level random coefficient on one alternative attribute
(a "taste shifter"). A constant-within-case random intercept cancels in
ranking likelihoods, so heterogeneity must attach to an attribute that
varies across alternatives.
}
\examples{
\dontrun{
fit <- fit_rank(rank ~ price + quality, case = ~ subject,
                random = ~ (0 + price | region), data = d)
}

}
