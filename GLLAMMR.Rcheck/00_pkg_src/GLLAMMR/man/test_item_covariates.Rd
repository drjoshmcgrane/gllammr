% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eirt_utilities.R
\name{test_item_covariates}
\alias{test_item_covariates}
\title{Test Item Covariate Effects}
\usage{
test_item_covariates(
  response_matrix,
  item_data,
  difficulty_formula = ~1,
  discrimination_formula = ~1,
  model = c("Rasch", "2PL", "GRM"),
  ...
)
}
\arguments{
\item{response_matrix}{Matrix of item responses}

\item{item_data}{Data frame of item-level covariates}

\item{difficulty_formula}{Formula for difficulty regression}

\item{discrimination_formula}{Formula for discrimination regression}

\item{model}{IRT model type}

\item{...}{Additional arguments passed to fit_eirt}
}
\value{
A list with:
  \item{full_model}{EIRT model with covariates}
  \item{null_model}{EIRT model without covariates (intercept only)}
  \item{comparison}{Model comparison results}
}
\description{
Test whether adding item covariates significantly improves model fit
}
\examples{
\dontrun{
item_data <- data.frame(
  word_freq = rnorm(20),
  length = rpois(20, 5)
)

# Test if word frequency matters
result <- test_item_covariates(
  responses,
  item_data,
  difficulty_formula = ~ word_freq + length,
  model = "Rasch"
)

print(result$comparison)
}

}
