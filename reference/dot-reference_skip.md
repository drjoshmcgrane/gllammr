# Signal that a reference-package fit failed (a platform artefact, not a gllammr defect). Caught by \[gllammr_validate()\] and turned into a skipped row rather than a failure.

Signal that a reference-package fit failed (a platform artefact, not a
gllammr defect). Caught by \[gllammr_validate()\] and turned into a
skipped row rather than a failure.

## Usage

``` r
.reference_skip(msg)
```
