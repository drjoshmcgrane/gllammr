# Compute R-squared for Item Parameter Regression

Calculate proportion of variance in item parameters explained by
covariates

## Usage

``` r
eirt_r_squared(object, parameter = c("difficulty", "discrimination"))
```

## Arguments

- object:

  A gllamm_eirt object

- parameter:

  Which parameter: "difficulty" or "discrimination"

## Value

R-squared value
