# Validate and auto-recode polytomous response matrices (shared by the Laplace and EM estimation paths)

Validate and auto-recode polytomous response matrices (shared by the
Laplace and EM estimation paths)

## Usage

``` r
validate_poly_responses(response_matrix)
```

## Value

list(response_matrix, n_categories_per_item, max_categories)
