% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/npml.R
\name{fit_npml}
\alias{fit_npml}
\title{Fit Two-Level GLMMs by Nonparametric Maximum Likelihood (NPML)}
\usage{
fit_npml(
  formula,
  data,
  k = 2,
  family = stats::gaussian(),
  weights = NULL,
  n_starts = 3,
  start = NULL,
  control = list()
)
}
\arguments{
\item{formula}{Model formula \code{y ~ x + (1 | group)}}

\item{data}{Data frame}

\item{k}{Number of mass points (default 2)}

\item{family}{gaussian(), binomial(), or poisson()}

\item{weights}{Optional observation weights}

\item{n_starts}{Random restarts (mixtures are prone to local optima)}

\item{start}{Optional starting values}

\item{control}{Optimization control list}
}
\value{
An object of class \code{gllamm_npml}
}
\description{
Replaces the normal random-intercept distribution with a discrete
distribution on \code{k} estimated mass points (locations and masses).
The marginal likelihood is an exact finite mixture - no integral
approximation - making NPML robust to misspecification of the
random-effects distribution.
}
\details{
The fixed-effects design drops its intercept: the mass-point locations
play the role of component intercepts (the usual NPML identification,
as in \pkg{npmlreg}). Masses are softmax-parameterized.
}
\examples{
\dontrun{
fit <- fit_npml(y ~ x + (1 | group), data = d, k = 3,
                family = stats::binomial())
}

}
