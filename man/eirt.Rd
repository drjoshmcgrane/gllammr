% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{eirt}
\alias{eirt}
\title{Explanatory IRT Family}
\usage{
eirt(
  item_data,
  difficulty_formula = ~1,
  discrimination_formula = ~1,
  threshold_formula = NULL,
  step_formula = NULL,
  step_data = NULL,
  model = c("Rasch", "2PL", "GRM", "PCM", "GPCM"),
  item_residuals = TRUE
)
}
\arguments{
\item{item_data}{Data frame of item covariates (one row per item)}

\item{difficulty_formula}{Item-covariate formula for difficulty}

\item{discrimination_formula}{Item-covariate formula for (log)
discrimination (2PL/GRM/GPCM)}

\item{threshold_formula}{Optional threshold regression (LPCM framework)}

\item{step_formula}{Optional step-level covariate formula (PCM/GPCM)}

\item{step_data}{Data frame for step_formula (one row per item-step cell)}

\item{model}{"Rasch", "2PL", "GRM", "PCM", or "GPCM"}

\item{item_residuals}{Random item residuals around the regression
(LLTM-plus-error; default TRUE)}
}
\value{
An object of class \code{eirt_family}
}
\description{
Create a family object for explanatory item response models through the
unified \code{gllamm()} interface. The response is the persons x items
matrix passed as the first argument of \code{gllamm()}; \code{data}
(optional) carries person-level variables and \code{random} person-level
random effects. See \code{\link{fit_eirt}}.
}
\examples{
\dontrun{
fit <- gllamm(resp, family = eirt(item_data,
                                  difficulty_formula = ~ btype + mode))
}

}
