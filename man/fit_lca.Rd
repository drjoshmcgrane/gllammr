% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/latent_class.R
\name{fit_lca}
\alias{fit_lca}
\title{Fit Latent Class Analysis Models}
\usage{
fit_lca(
  formula,
  data = NULL,
  nclass = 2,
  weights = NULL,
  method = c("em", "tmb"),
  start = NULL,
  control = list()
)
}
\arguments{
\item{formula}{Formula specifying manifest variables (can be a matrix)}

\item{data}{Data frame containing variables}

\item{nclass}{Number of latent classes}

\item{weights}{Optional vector of case weights (one per observation)}

\item{method}{Estimation method: "em" (default; closed-form EM with
Ramsay acceleration, the poLCA algorithm) or "tmb" (direct
quasi-Newton on the marginal likelihood via TMB)}

\item{start}{Optional starting values}

\item{control}{Control parameters}
}
\value{
An object of class \code{gllamm_lca}
}
\description{
Fit latent class models to categorical data
}
\examples{
\dontrun{
# Simulate 2-class data
set.seed(123)
n <- 500

# Class 1: high probability of yes
class1_probs <- c(0.8, 0.7, 0.9, 0.75)
# Class 2: low probability of yes
class2_probs <- c(0.2, 0.3, 0.1, 0.25)

# Generate data
true_class <- sample(1:2, n, replace = TRUE, prob = c(0.6, 0.4))
data <- matrix(NA, n, 4)
for (i in 1:n) {
  probs <- if (true_class[i] == 1) class1_probs else class2_probs
  data[i, ] <- rbinom(4, 1, probs)
}
colnames(data) <- paste0("Item", 1:4)

# Fit 2-class model
fit <- fit_lca(data, nclass = 2)
summary(fit)
}

}
