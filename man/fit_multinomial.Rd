% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ordinal.R
\name{fit_multinomial}
\alias{fit_multinomial}
\title{Fit Multinomial Regression Models with Random Effects}
\usage{
fit_multinomial(
  formula,
  data,
  reference = NULL,
  start = NULL,
  control = list()
)
}
\arguments{
\item{formula}{Formula with syntax: y ~ x + (terms | group)}

\item{data}{Data frame}

\item{reference}{Reference category (default: first level)}

\item{start}{Optional starting values}

\item{control}{Control parameters}
}
\value{
An object of class \code{gllamm_multinomial}
}
\description{
Fit baseline category logit models for nominal (unordered) responses
}
\details{
For nominal response Y with K categories, using baseline category logit:

\deqn{P(Y = k | x) = \frac{\exp(x'\beta_k)}{1 + \sum_{j=1}^{K-1} \exp(x'\beta_j)}}

where category 0 is the reference with \eqn{\beta_0 = 0}.
}
\examples{
\dontrun{
# Simulate multinomial data
data$choice <- factor(sample(c("A", "B", "C"), 100, replace = TRUE))

# Fit multinomial model
fit <- fit_multinomial(choice ~ price + quality + (1 | person),
                       data = data)
summary(fit)
}

}
